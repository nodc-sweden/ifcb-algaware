% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_plots.R
\name{create_ctd_region_figure}
\alias{create_ctd_region_figure}
\title{Create a regional CTD composite figure (all standard stations)}
\usage{
create_ctd_region_figure(
  ctd_data_full,
  lims_data_full = NULL,
  chl_stats,
  standard_stations,
  region,
  current_year,
  force_two_columns = FALSE
)
}
\arguments{
\item{ctd_data_full}{Data frame from \code{read_cnv_folder_all()}.}

\item{lims_data_full}{Data frame from \code{read_lims_data_all()}, or NULL.}

\item{chl_stats}{Data frame from \code{load_chl_statistics()}.}

\item{standard_stations}{Data frame from \code{load_standard_stations()}.}

\item{region}{Character region name.}

\item{current_year}{Integer year.}

\item{force_two_columns}{Logical; always use a two-column layout even
without LIMS data (right column filled with spacers).  Default FALSE.}
}
\value{
A patchwork object, or NULL if there are no data for the region.
}
\description{
Produces a multi-station patchwork figure for one geographic region.
Each row shows one station: left panel is the CTD fluorescence profile
(0–50 m, deduplicated casts, shared x-scale), right panel is the Chl-a
time series with smooth spline historical statistics.
}
\details{
When \code{force_two_columns = TRUE} (used for report output), a spacer
column is added even when no LIMS data are available, keeping the profile
panels at a consistent width across all regions.

Month labels appear only on the bottom station row (English abbreviations,
locale-independent).
}
