# Add a heatmap section to the report

Add a heatmap section to the report

## Usage

``` r
add_heatmap_section(
  doc,
  wide_data,
  taxa_lookup,
  title,
  fig_num,
  cleanup,
  sample_counts = NULL
)
```
