# Add station report sections to document

Add station report sections to document

## Usage

``` r
add_station_sections(
  doc,
  station_summary,
  taxa_lookup = NULL,
  use_llm = FALSE,
  phyto_groups = NULL,
  llm_provider = NULL,
  on_llm_progress = NULL,
  unclassified_fractions = NULL
)
```
