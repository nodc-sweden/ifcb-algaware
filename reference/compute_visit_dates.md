# Compute representative visit dates

For each station visit, determines the most common sample date.

## Usage

``` r
compute_visit_dates(all_data)
```

## Arguments

- all_data:

  Data.frame with visit_id, STATION_NAME, sample_date columns.

## Value

Data.frame with visit_id, STATION_NAME, visit_date columns.
