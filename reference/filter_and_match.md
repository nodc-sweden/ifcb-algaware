# Filter and match metadata to stations

Filter and match metadata to stations

## Usage

``` r
filter_and_match(
  dashboard_metadata,
  selection_mode,
  cruise,
  date_range,
  extra_stations
)
```
