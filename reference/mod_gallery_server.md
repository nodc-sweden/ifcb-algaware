# Gallery Module Server

Gallery Module Server

## Usage

``` r
mod_gallery_server(id, rv, config)
```

## Arguments

- id:

  Module namespace ID.

- rv:

  Reactive values for app state.

- config:

  Reactive values with settings.

## Value

NULL (side effects only).
