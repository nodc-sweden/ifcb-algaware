# Settings Module Server

Settings Module Server

## Usage

``` r
mod_settings_server(id, config)
```

## Arguments

- id:

  Module namespace ID.

- config:

  Reactive values object for settings.

## Value

NULL (side effects only).
